import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.raminarmanfar.vocabtrainer',
  appName: 'Vokabel Trainer',
  webDir: 'dist/vocabulary-training-app/browser'
};

export default config;
